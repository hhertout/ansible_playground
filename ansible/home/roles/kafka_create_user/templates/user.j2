apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: "{{kafka_username}}"
  namespace: "{{k8s_namespace}}"
  labels:
    strimzi.io/cluster: "{{kafka_cluster_name}}"
spec:
  authentication:
    type: scram-sha-512