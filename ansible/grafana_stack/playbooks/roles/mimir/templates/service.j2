apiVersion: v1
kind: Service
metadata:
  namespace: {{ namespace }}
  name: {{ mimir.name }}-service
spec:
  type: ClusterIP
  selector:
    app: {{ mimir.name }}
  ports:
    - protocol: TCP
      port: {{ mimir.specs.exposed_port }}
      targetPort: {{ mimir.specs.container_port }}